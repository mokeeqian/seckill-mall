spring:
  application:
    name: mall-coupon
  cloud:
    nacos:
      # nacos 配置中心
      config:
        server-addr: 127.0.0.1:8848
        # 命名空间，用以环境隔离（默认是Public）
        # 可以基于环境隔离，也可以基于微服务隔离
        namespace: coupon